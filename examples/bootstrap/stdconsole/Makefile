PLATFORM ?= exp430fr5739
MODULES=$(MODULES_PLATFORM) $(MODULES_CONSOLE)
WITH_INPUT ?= 1
ifneq (,$(WITH_INPUT))
AUX_CPPFLAGS += -DWITH_INPUT
endif # WITH_INPUT
SRC=main.c
include $(BSP430_ROOT)/make/Makefile.common
